minPressureToKeepContact = 0.5;
minPressureToRegainContact = 0.55;
minPressurePerFootToRegainContact = 0.25;
maxGyroYToRegainContact = 40deg;
maxTimeWithoutPressure = 100;
minTimeWithPressure = 150;
use_pikmin_sounds = true;    // they are pretty high-pitched, so turn this off if there is any fear of mistaking them for whistles
